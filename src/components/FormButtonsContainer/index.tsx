import React from 'react'

import { Container } from './styles'

const FormButtonsContainer: React.FC = ({ children }) => {
  return <Container>{children}</Container>
}

export default FormButtonsContainer
